import { CompanyDirectory } from "@/components/crm/company-directory";

export const metadata = { title: "Companies — Algo PBX" };

export default function AdminCompaniesPage() {
  return <CompanyDirectory />;
}
