; =========================================
; ALGO PBX — QUEUE CONFIGURATION
; =========================================
; This file was missing entirely until now, which was a hard runtime bug:
; extensions.conf's [from-dinstar] context routes every inbound call to
; Queue(support_queue,...), but with no queues.conf the queue is undefined
; and the Queue() application fails — i.e. no inbound call could ever be
; serviced. Nothing downstream (wallboard, queue stats, agent routing) can
; work without this.

[general]
; Keep dynamically-added members (AMI QueueAdd / AddQueueMember) across an
; Asterisk restart. Matters because Phase A provisioning adds agents at
; runtime rather than editing this file.
persistentmembers = yes
autofill = yes
; S6: `monitor-type = MixMonitor` was REMOVED here. It made Queue() start
; its own recording automatically for every queued (inbound) call — which
; (a) bypassed the global recording toggle (extensions.conf's
; RECORDING_ENABLED gate can't reach a queue.conf directive) and (b)
; double-recorded, since [from-dinstar] already starts a MixMonitor on the
; same ${UNIQUEID} before Queue(). Inbound recording is now started once,
; gated, in [from-dinstar]. Leave this commented, not deleted, as the
; explicit record of why it must not come back.
; monitor-type = MixMonitor

; =========================================
; SUPPORT QUEUE
; Name MUST stay in sync with extensions.conf's Queue(support_queue,...)
; and with the Queue.name row in prisma/schema.prisma.
; =========================================
[support_queue]
; NOTE: Prisma's Queue.strategy column also stores a strategy and nothing
; syncs the two — Asterisk reads it from here, the web UI reads it from
; Postgres, and they can silently disagree (extension provisioning had this
; same class of gap until Phase A's config-generation fixed it for
; pjsip.conf; queues.conf isn't generated yet). Tracked in LLM.md; do not
; assume changing the DB value affects call routing.
strategy = ringall

; Ring each agent for 15s before moving on; wait 5s between ring cycles.
timeout = 15
retry = 5
; Give an agent 10s after a call to finish notes before offering the next.
wrapuptime = 10
; Loop D1 — was 0 (unlimited). The Dinstar gateway has exactly 4 GSM SIM
; ports total (the real, hard concurrency ceiling of this entire system,
; regardless of how the VM or this queue is sized) — an unbounded queue
; let callers pile up against a trunk that could never actually answer
; them. A caller hitting this cap gets QUEUESTATUS=FULL, routed to the
; office overflow mailbox by extensions.conf's [from-dinstar] context
; rather than an indefinite hold.
maxlen = 4
; Hold music class — defined in musiconhold.conf (Phase B).
musicclass = default
; Don't offer a call to an agent who is already on one.
ringinuse = no

; Loop D1 — was joinempty=yes (dev-friendly, flagged in-file at the time
; as "REVISIT FOR PRODUCTION"). A caller entering an unstaffed queue with
; joinempty=yes just waited in silence forever (moh/default/ ships no
; audio by design — see that directory's README — until an operator adds
; real files) with zero prospect of an answer. joinempty=no now routes
; that caller to the office overflow mailbox instead (QUEUESTATUS=JOINEMPTY,
; handled in extensions.conf's [from-dinstar]) — a real outcome instead of
; indefinite hold.
joinempty = no
leaveempty = yes

; Periodic "thanks for holding" style announcements. announce-frequency=0
; disables the position announcement (still no real prompt for THAT one).
; periodic-announce-frequency was already set (60s) but had no file to
; actually play — periodic-announce below closes that now that a real
; prompt exists (pbx_configs/sounds/please-hold.wav, mounted at
; /var/lib/asterisk/sounds/en/custom/ — see docker-compose.yml).
announce-frequency = 0
periodic-announce-frequency = 60
periodic-announce = custom/please-hold

; No static `member =>` line here anymore — membership is fully dynamic
; now (src/lib/queue-membership.ts): an agent is added via AMI QueueAdd
; when their extension is provisioned (POST /api/admin/users), removed on
; disable, and reconciled with reconcileQueueMembership() so a fresh
; Asterisk boot picks up whatever agents already exist in Postgres. A
; queue with zero members is expected and correct on a brand-new install
; before the first agent is created — joinempty=yes above is what keeps
; that from hard-failing inbound calls in the meantime.
