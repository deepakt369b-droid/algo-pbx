; Registers the unixODBC DSN (odbc.ini's [algopbx_pg]) with Asterisk itself,
; so func_odbc.conf's DNC_CHECK function (dsn=algopbx-dnc below) can use it.
[algopbx-dnc]
enabled => yes
dsn => algopbx_pg
pre-connect => yes
; Read-only lookups only — this connection is never used to write.
