; GENERATED FILE — DO NOT HAND-EDIT.
; Rendered by src/lib/pjsip-config.ts from the Extension table in Postgres.
; Hand edits will be silently overwritten the next time an extension is
; created, updated, or removed. To change static config (transports, the
; Dinstar trunk), edit pjsip-base.conf instead — see docker-compose.yml's
; comment on how the two files are #included together.
;
; This seed file was regenerated by hand to match src/lib/pjsip-config.ts's
; current output (alaw-first codec order, direct_media=no, rewrite_contact,
; rtp_symmetric, rtp_timeout) after the pre-trial voice-quality fixes — the
; real source of truth is Postgres via /admin/extensions; this file exists
; only so `docker compose up` has something valid to parse before any
; extension has been provisioned through the UI.

[1001]
type=endpoint
transport=transport-wss
context=from-agent
disallow=all
allow=alaw,ulaw,opus
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_cert_file=/etc/asterisk/keys/asterisk.crt
dtls_private_key_file=/etc/asterisk/keys/asterisk.key
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rewrite_contact=yes
rtp_symmetric=yes
direct_media=no
rtp_timeout=30
rtp_timeout_hold=300
auth=1001-auth
aors=1001-aor

[1001-auth]
type=auth
auth_type=userpass
username=1001
password=REPLACE_ME_AGENT_1001_PASSWORD

[1001-aor]
type=aor
max_contacts=2
remove_existing=yes

[2001]
type=endpoint
transport=transport-udp
context=from-agent
disallow=all
allow=alaw,ulaw
direct_media=no
rtp_timeout=30
rtp_timeout_hold=300
auth=2001-auth
aors=2001-aor

[2001-auth]
type=auth
auth_type=userpass
username=2001
password=REPLACE_ME_PHONE_2001_PASSWORD

[2001-aor]
type=aor
max_contacts=1
